name: Release & Desktop Binaries

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release Version Tag (e.g. v1.0.0)'
        required: true
        default: 'v1.0.0'
      draft:
        description: 'Publish as Draft'
        type: boolean
        default: false
      prerelease:
        description: 'Publish as Prerelease'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  build:
    name: Build & Release All Desktop Binaries
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: |
          node -e "const fs = require('fs'); fs.rmSync('package-lock.json', {force: true});"
          npm install --include=optional

      - name: Run Linter & Type Check
        run: npm run lint

      - name: Build Web App & All Desktop Binaries
        run: npm run build:neu

      - name: List Generated Binaries
        run: |
          echo "=== Generated files in dist/reststudio ==="
          find dist/reststudio -type f -ls

      - name: Upload Linux ARM64
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Linux-ARM64
          path: dist/reststudio/RestStudio-Linux-ARM64
          if-no-files-found: warn

      - name: Upload Linux ARMhf
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Linux-ARMhf
          path: dist/reststudio/RestStudio-Linux-ARMhf
          if-no-files-found: warn

      - name: Upload Linux x64
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Linux-x64
          path: dist/reststudio/RestStudio-Linux-x64
          if-no-files-found: warn

      - name: Upload Mac ARM64
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Mac-ARM64
          path: dist/reststudio/RestStudio-Mac-ARM64.zip
          if-no-files-found: warn

      - name: Upload Mac Universal
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Mac-Universal
          path: dist/reststudio/RestStudio-Mac-Universal.zip
          if-no-files-found: warn

      - name: Upload Mac x64
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Mac-x64
          path: dist/reststudio/RestStudio-Mac-x64.zip
          if-no-files-found: warn

      - name: Upload Windows x64 Exe
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Windows-x64-Exe
          path: dist/reststudio/RestStudio-Windows-x64.exe
          if-no-files-found: warn

      - name: Upload Windows x64 Zip
        uses: actions/upload-artifact@v4
        with:
          name: RestStudio-Windows-x64-Zip
          path: dist/reststudio/RestStudio-Windows-x64.zip
          if-no-files-found: warn

      - name: Create or Update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version || (startsWith(github.ref, 'refs/tags/') && github.ref_name) || 'nightly' }}
          name: RestStudio Desktop ${{ github.event.inputs.version || (startsWith(github.ref, 'refs/tags/') && github.ref_name) || 'Nightly Build' }}
          overwrite_files: true
          target_commitish: ${{ github.sha }}
          draft: ${{ github.event.inputs.draft || false }}
          prerelease: ${{ github.event.inputs.prerelease || github.ref_name == 'main' || true }}
          generate_release_notes: true
          fail_on_unmatched_files: false
          files: |
            dist/reststudio/RestStudio-Linux-*
            dist/reststudio/RestStudio-Mac-*.zip
            dist/reststudio/RestStudio-Windows-*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
