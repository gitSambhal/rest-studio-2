{
  "name": "reststudio-desktop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "tauri:dev": "npm run build && tauri dev",
    "tauri:build": "npm run build && tauri build",
    "neu:dev": "npm run build && npx @neutralinojs/neu update && (chmod +x bin/* 2>/dev/null || true) && (xattr -cr bin/ 2>/dev/null || true) && npx @neutralinojs/neu run",
    "neu:build": "npm run build && npx @neutralinojs/neu update && npx @neutralinojs/neu build && (chmod +x bin/* 2>/dev/null || true) && (xattr -cr bin/ 2>/dev/null || true) && node scripts/make-mac-app.js && node scripts/patch-win-executables.js",
    "build": "node scripts/generate-icons.js && vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "build:tauri": "npm run build && tauri build",
    "build:neu": "npm run build && npx @neutralinojs/neu update && npx @neutralinojs/neu build && (chmod +x bin/* 2>/dev/null || true) && (xattr -cr bin/ 2>/dev/null || true) && node scripts/make-mac-app.js && node scripts/patch-win-executables.js",
    "start": "node dist/server.cjs",
    "preview": "vite preview",
    "clean": "node -e \"const fs=require('fs'); ['dist', 'src-tauri/target', '.neu', 'server.js'].forEach(d => fs.rmSync(d, {recursive: true, force: true}));\"",
    "prebuild": "npm run clean",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "axios": "^1.19.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "7zip-bin": "^5.2.0",
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/cli": "^2.11.4",
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.1.14",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
